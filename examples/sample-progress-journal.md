# Sample Engineering Progress & Retrospective Journal

> **Note**: This is a real-world reference journal entry preserved from a production project (`Shelf` - a full-stack book management app using React 19, Vite, TanStack Router, Express, PostgreSQL, Drizzle ORM, and Supabase Auth). Use this as a quality standard for your own entries in `notes/progress-journal.md`.

---

| Date | Focus / Challenge | Key Architectural Insights & Mental Models | Trade-Offs & Decisions Made | Next High-Leverage Action | Tags |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 2026-08-23 | Upgrading PromptKit to Senior Developer & Modern Architecture Standard | Established 3-tier Socratic mentoring, spec-driven architecture planning, hypothesis-driven debugging, and modern UI/UX design token workflows. | Chose modular markdown-first architecture over rigid CLI binaries to ensure universal compatibility across all AI IDEs and CLIs. | Implement first system design spike and test tutor workflow with progressive hints. | `#architecture` `#mentorship` `#promptkit` |
| 2026-08-27 | Drizzle ORM schema design & migrations for shelf board #5 (users, books, user_books) | Split tables by "who owns the fact": shared reference data (`books`) vs personal layer (`user_books`). Composite PK `(userId, bookId)` enforces uniqueness at DB level. Migration generated via `drizzle-kit generate` and successfully applied to PostgreSQL instance. | Chose Drizzle over Prisma and raw `pg`. Aligned `.env` credentials with Docker Compose and Drizzle Kit. | Implement Express REST API endpoints in `/api/library` (board #6) or Auth middleware (#7). | `#database` `#postgresql` `#drizzle` `#orm` `#shelf` |
| 2026-08-27 | Supabase Auth middleware & protected route for shelf board #7 (PR #27) | Express middleware intercepts `Authorization: Bearer <token>`, validates token signature and expiration via `@supabase/supabase-js`, and attaches strongly typed `User` to `req.user`. Standardized JSON 401s for missing/invalid tokens. | Switched from Auth0 to Supabase Auth for simpler developer experience, direct PostgreSQL integration, and intuitive frontend/backend SDKs. Extended Request via `AuthenticatedRequest` interface avoiding namespace linter issues. | Merge PR #27, branch `feature/library-api` to build REST endpoints for `/api/library` (board #6). | `#auth` `#supabase` `#express` `#middleware` `#shelf` |
| 2026-08-30 | Express Request Declaration Merging & Route Handler Type Alignment | Resolved TypeScript route handler overload mismatches (caused by function parameter contravariance with custom `AuthenticatedRequest`) by globally augmenting Express's `Request` interface via `declare global { namespace Express { interface Request { user?: User } } }`. | Replaced ad-hoc `AuthenticatedRequest` wrapper with global module augmentation, enabling clean inference on all route handlers without type collisions. | Test library REST API endpoints end-to-end against local server and Supabase tokens. | `#typescript` `#express` `#module-augmentation` `#middleware` `#shelf` |
| 2026-08-31 | Full CRUD REST API for `/api/library` (GET, POST, PATCH, DELETE) & Param Middleware | Completed full RESTful lifecycle for user library management with Drizzle ORM. Centralized parameter validation using Express `router.param('bookId')` to eliminate route-level duplication and prevent "headers already sent" ordering bugs. Handled 204 No Content for deletion and 404 for missing records. | Chose Express `router.param` over manual inline parameter guards to keep route handlers focused purely on business orchestration and status codes. | Add runtime request validation (Zod schemas) or begin frontend router/data integration. | `#express` `#rest-api` `#drizzle` `#middleware` `#shelf` |
| 2026-09-03 | Supabase Auth Client Integration, State Encapsulation & Bearer Token Fetcher (#11) | Initialized `@supabase/supabase-js` singleton in Vite frontend. Built `AuthProvider` subscribing to `onAuthStateChange` with separate `useAuth` hook adhering to Fast Refresh rules. Encapsulated login/signup toggle logic inside `AuthGuard` container rather than prop-drilling, and created `apiFetch` wrapper injecting dynamic `Authorization: Bearer <token>` headers. Verified full live login/logout flow in browser. | Separated hook from context file to satisfy Vite HMR requirements; encapsulated auth state in `AuthGuard` to achieve zero-prop route protection. | Merge PR #29 and implement TanStack Router layouts (#12). | `#react` `#supabase` `#auth` `#context` `#vite` `#shelf` |
| 2026-09-04 | TanStack Router Layout Architecture, Pathless Auth Shell & Param Validation (#12) | Configured `@tanstack/react-router` with Vite plugin code splitting. Built persistent `__root.tsx` layout with header nav, active link states, and footer. Composed pathless `_authenticated.tsx` route shell protecting private views via `<AuthGuard>`, and created dynamic `/library/$bookId` details route with numeric parameter validation. Silenced Fast Refresh route export warnings in ESLint. | Chose TanStack file-based routing over traditional code routes for auto-generated `routeTree.gen.ts` type-safety. Used pathless `_authenticated` layout to avoid polluting URL structure. | Implement TanStack Query data fetching and caching for library routes (#9/#15). | `#react` `#tanstack-router` `#routing` `#type-safety` `#shelf` |
| 2026-09-04 | Debounced Open Library Search Hook, Derived State & AbortController (#13) | Created generic `useDebounce<T>` hook with 300ms delay and cleanup. Built pure payload mapper `mapOpenLibraryDocToBook` translating Open Library's raw payloads into typed `BookSearchResult` contracts. Implemented `useBookSearch` hook using modern React 19 derived state to eliminate cascading renders, and guarded against race conditions using `AbortController` cancellation signals. | Used derived state during render rather than calling synchronous `setState` in effects to comply with React 19 rules; decoupled data mapping from UI hook for pure testability. | Wire search hook to landing route (`index.tsx`) and add book action to personal library (#14/#15). | `#react` `#hooks` `#debounce` `#abortcontroller` `#openlibrary` `#shelf` |
